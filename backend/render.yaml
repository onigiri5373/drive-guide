services:
  - type: web
    name: drive-guide-api
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 3001
      - key: ALLOWED_ORIGINS
        sync: false
